# กติกาการทำงานของโปรเจกต์นี้

> ไฟล์นี้คือกติกาที่ผู้ร่วมงานทุกคน (รวมถึง Claude) ต้องทำตามทุกครั้ง
> ส่วนรายละเอียดของระบบอยู่ที่ `docs/00-กระบวนการทำงานทั้งหมด.md`

## 1. ภาษา

- **สื่อสารเป็นภาษาไทยเสมอ** ทั้งคำถาม คำตอบ คำอธิบาย บทสรุป และคอมเมนต์ในโค้ดส่วนสำคัญ
- ใช้ภาษาอังกฤษเฉพาะชื่อตัวแปร ชื่อฟังก์ชัน ชื่อไฟล์ และไวยากรณ์ของภาษาโปรแกรม
- **เขียนให้เป็นภาษามนุษย์** อธิบายทีละขั้น ไม่ใช้ศัพท์เทคนิคโดยไม่จำเป็น
  ไม่กระโดดไปมาระหว่างเรื่อง ทำให้จบทีละเรื่อง

## 2. เวลาจะให้ผู้ใช้รันคำสั่ง ต้องแจ้งรายละเอียดครบตั้งแต่เริ่ม

**ห้ามโยนคำสั่งมาลอย ๆ** ทุกครั้งที่ขอให้ผู้ใช้พิมพ์คำสั่ง ต้องบอกให้ครบทุกข้อนี้
ตั้งแต่ข้อความแรก ไม่ใช่รอให้ถามแล้วค่อยตอบทีละอย่าง

| ต้องบอก | ตัวอย่าง |
|---|---|
| **รันที่ไหน** | เปิดโปรแกรมอะไร (Command Prompt / PowerShell / Terminal) |
| **อยู่โฟลเดอร์ไหน** | เขียน path เต็ม เช่น `C:\Users\TIKKIE\Desktop\ai project\register-app` |
| **คำสั่งเต็ม** | คัดลอกวางได้ทันที ไม่ต้องเติมอะไรเอง |
| **คำสั่งนี้ทำอะไร** | อธิบายเป็นภาษาคนว่าจะเกิดอะไรขึ้น และทำไมต้องทำ |
| **ใช้เวลาประมาณเท่าไร** | ถ้านานกว่า 10 วินาที ต้องบอก ไม่งั้นผู้ใช้จะคิดว่าค้าง |
| **ถ้าสำเร็จจะเห็นอะไร** | ข้อความหรือตัวเลขที่ควรเห็นบนจอ เพื่อให้รู้ว่าผ่านจริง |
| **ถ้าไม่สำเร็จให้ทำอะไร** | ให้ส่งอะไรกลับมา และห้ามทำอะไรต่อ |
| **ลำดับ** | ถ้ามีหลายคำสั่ง ต้องบอกว่าอันไหนก่อนหลัง และห้ามข้าม |

ถ้ามีขั้นตอนที่ทำผิดลำดับแล้วเสียหาย (เช่น deploy ก่อนรัน migration)
ต้องเตือนไว้ตั้งแต่ต้น พร้อมบอกว่าจะเกิดอะไรขึ้นถ้าทำผิดลำดับ

## 3. ห้ามเริ่มเขียนโค้ดทันที

- เริ่มที่ขั้นตอนที่ 0 (ทำความเข้าใจและวางแผน) แล้ว**รอคำยืนยันก่อนทุกเฟส**
- จบแต่ละเฟสให้หยุด รายงาน แล้วถามก่อนไปต่อ

## 4. ไม่แน่ใจให้ถาม

- หยุดแล้วถามเป็นภาษาไทย พร้อม**ตัวเลือกที่แนะนำและเหตุผล** ห้ามเดา
- ถ้าเจอว่าสิ่งที่ผู้ใช้สั่งมีปัญหา ให้บอกตรง ๆ สั้น ๆ แล้วทำต่อ ไม่เงียบ

## 5. ต้องมีเช็กลิสต์ทวนความสมบูรณ์ทุกครั้งที่ส่งมอบงาน

บอกให้ครบว่า **สั่งอะไรมาบ้างทีละข้อ · สถานะ (✅ / ⚠️ / ❌) · หลักฐาน · เหลืออะไร**

## 6. ห้ามบอกว่า "เสร็จแล้ว" ถ้ายังไม่ได้ตรวจว่ารันได้จริง

- ต้องรันจริงและเห็นผลจริงก่อนเสมอ
- **ห้ามอ้างว่า "เป็นปัญหาเดิมอยู่แล้ว" โดยไม่พิสูจน์** ให้ถอยโค้ดของตัวเองออก
  (`git stash`) แล้วรันซ้ำเพื่อยืนยันก่อน
- ถ้าเทสต์ล้ม ให้รายงานตามจริงพร้อมข้อความที่ได้ ห้ามปิดบัง

## 7. ขอบเขตของไฟล์

- **ห้ามสร้างหรือแก้ไขไฟล์นอกโฟลเดอร์โปรเจกต์นี้โดยไม่ขออนุญาตก่อน**
- `.gitignore` ต้องกัน `node_modules` · `.env` · ไฟล์ build · และข้อมูลผู้ลงทะเบียนจริง

## 8. Git

- **สาขาสำหรับพัฒนา: `claude/event-registration-step-0-i9ulcz`**
- **ห้าม push ไปสาขาอื่น (รวมถึง `main`) โดยไม่ได้รับอนุญาตเป็นครั้ง ๆ ไป**
- ห้าม commit รหัสผ่าน คีย์ หรือความลับใด ๆ ลง repo เด็ดขาด

## 9. ก่อนขึ้นระบบจริง

ยังมีงานที่ผู้ใช้ต้องทำเองและ Claude ทำแทนไม่ได้ ดูรายการล่าสุดได้ที่
`docs/security-report.md` และ `docs/UAT-checklist.md`
